{
  "scripts": {
    "copy": "rm -rf assets && rm -rf blog && rm -rf code && mv -f ./build/* .",
    "status": "git status",
    "add": "git add",
    "add-all": "git add -A",
    "commit": "git commit -a -m",
    "push": "git push",
    "return": "git checkout master",
    "deploy": "npm run push && npm run return"
  }
}
