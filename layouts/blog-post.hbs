<!DOCTYPE html>
<html>
{{> html-head }}

<body>
    {{> header }}

    <div id="main">
        <div class="container">

            <article>

                <div class="humongous {{#unless cover-img}}no-cover{{/unless}}{{#if cover-img}}cover-img{{/if}}" {{#if cover-img}}style="background-image: url({{root}}/assets/img/{{cover-img}});"{{/if}}>
                    <h1 class="hello">{{title}}</h1>
                    <span class="datetime"><time>Posted on {{posted}}</time></span> / <span class="readtime">{{wordCount}} words - about a {{readingTime}} read</span>
                </div>
                <div class="content">
                 {{{ contents }}}
                </div>

            </article>

        </div>
    </div>

</body>
</html>
